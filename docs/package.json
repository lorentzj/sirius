{
  "name": "srs-docs",
  "version": "1.0.0",
  "description": "Docs for the Sirius programming language",
  "license": "MIT",
  "scripts": {
    "build": "rm -f public/js/worker.*.js.map public/js/worker.*.js && FORCE_COLOR=1 npx parcel build ts/index.ts --dist-dir public/js && FORCE_COLOR=1 npx parcel build ts/editor.ts --dist-dir public/js",
    "watch": "watch --color npm run build",
    "serve": "cd public && miniserve --tls-cert ~/mkcert/lan.pem --tls-key ~/mkcert/lan-key.pem --header \"Cross-Origin-Embedder-Policy: require-corp\" --header \"Cross-Origin-Opener-Policy: same-origin\" ."
  },
  "dependencies": {
    "@codemirror/language": "^6.12.4",
    "codemirror": "^6.0.2",
    "z3-solver": "^5.1.0"
  }
}

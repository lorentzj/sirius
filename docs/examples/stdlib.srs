fn trig_fns(choice: i64) -> f64 -> f64 {
    if choice == 0 {
       return sin;
    }

    if choice == 1 {
        return cos;
    }

    return tan;
}

fn main() {
    print trig_fns(0)(pi/2);
    print trig_fns(1)(pi/2);
    print trig_fns(2)(pi/2);
}